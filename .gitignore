# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects sinh tự động bởi `expo prebuild` (không commit — tạo lại khi cần)
/android
/ios

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# env / secrets
.env
.env*.local

# Firebase config (chứa key cấu hình — với EAS Build phải nạp qua file secret, xem README)
google-services.json
GoogleService-Info.plist

# macOS
.DS_Store
*.pem

# IDE
.idea/
.vscode/
